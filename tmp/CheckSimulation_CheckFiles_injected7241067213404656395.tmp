package X.X.X;

import X.X.X.X;

public class X extends X {
    private final X X;
    private final X X;
    private final X X;

    public X (X X, X X, X X) {
        X.X = X;
        X.X = X;
    }

    public X X () {
        return X;
    }

    public X X () {
        return X;
    }

    public X X () {
        return X;
    }

    private static final X X = X.X.X.X (new X (0));

    public X X () {
        X X = new X ();
        X.X (X);
        if (X >= 0) {
            X.X (0);
        }
        X.X (X);
        X.X (X);
        if (X >= 0) {
            X.X (X);
            for (X X = 0;
            X < X; X ++) X.X (0);

            X.X (0);
        }
        return X.X ();
    }

}

