package X.X.X.X.X.X.X;

import X.X.X;

import X.X.X;

public class X extends X {

    public X () {
        X (0, new X (X.X, 0, 0));
    }

    public X (X X) {
        X (0, X != 0 ? X : new X (X.X, 0, 0));
    }

    public X (X X, X X) {
        X (0, X != 0 ? X : new X (X.X, 0, 0));
        X (X);
    }

    public X (X X, X X) {
        X (0, X != 0 ? X : new X (X.X, 0, 0));
        X (X);
    }

    public X X (X X) {
        X.X (X != 0 ? X : new X (X.X, 0, 0));
    }

}

