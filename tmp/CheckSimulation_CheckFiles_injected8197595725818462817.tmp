package X.X.X.X.X.X.X;

public class X extends X {

    X () {
    }

    public X (X X) {
        X (X.X.X.X.X.X.X.X, X);
    }

    public X X () {
        return new X (X);
    }

    public X X (X X) {
        if (X == X) X.X (X);

        if (X == X) X.X (X);

        if (X == X) X.X (X);

        if (X == X) X.X (X);

    }

}

