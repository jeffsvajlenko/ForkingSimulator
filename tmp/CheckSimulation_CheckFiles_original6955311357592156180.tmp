package X.X.X.X.X.X.X.X.X;

import X.X.X.X.X.X.X.X.X;

public class X extends X {

    public X (X X, X X) {
        X (X);
        if ((X () != X.X) && (X () != X.X) && (X () != X.X)) {
            throw new X (0);
        }
        X = X;
    }

    final X X () {
        return X;
    }

    public X X () {
        if (X () == X.X) return X.X ();
        else return 0;

    }

    protected X X (X X) {
        X.X (X.X ());
    }

    protected X X (X X) {
        X.X (X.X ());
    }

    private X X;
};

