package X.X.X.X;

import X.X.X;

import X.X.X;

import X.X.X;

import X.X.X;

import X.X.X.X;

import X.X.X.X.X;

class X extends X {

    public X (X X) {
        X (X);
    }

    @X
    protected X X () {
        if (X == X) X.X (X.X ());

        if (X == X) X.X (X.X ());

        if (X == X) X.X (0);

        X.X (X.X);
        X X = X.X ();
        if (X == - 0) {
            if (X == X) X.X ();

        } else {
            if (X == X) X.X (X);

            if (X == X) X.X (X);

        }
        if (X == X) X ();

    }

    @X
    protected X X (X X, X X, X X, X X) {
        X X = X.X ();
        if (X instanceof X) {
            X X = (X) X;
            if (X.X != 0) {
                X X = X.X;
                return X.X (X + X.X, X + X.X, X - X.X - X.X, X - X.X - X.X);
            }
        }
        return X.X (X, X, X, X);
    }

}

