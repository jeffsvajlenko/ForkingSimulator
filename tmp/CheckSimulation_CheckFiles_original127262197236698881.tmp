package X.X.X.X;

import X.X.X.X.*;

import X.X.*;

public class X {
    static class X implements X {

        X () {
        }

        public X X () {
        }

    }

    static class X implements X {
        private X < X > X = new X < X > ();

        public X (X < X > X) {
            X.X.X (X);
        }

        public X (X...X) {
            for (X X : X) X.X.X (X);

        }

        public X X () {
            for (X X : X) X.X ();

        }

    }

    public final static X X = new X ();

    public static X X (X...X) {
        return new X (X);
    }

    public static X X (X < X > X) {
        return new X (X);
    }

}

