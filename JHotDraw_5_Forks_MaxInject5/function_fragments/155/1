        public boolean isDefined(Object key) {
            Object[] X1 = attributes;
            int n = X1.length;
            for (int i = 0; i < n; i += 2) {
                if (key.equals(X1[i])) {
                    return true;
                }
            }
            return false;
        }
