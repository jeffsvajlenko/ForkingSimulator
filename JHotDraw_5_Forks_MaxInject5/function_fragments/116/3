    public Segment getSegment() {
        synchronized(this) {
            int size = segments.size();

            if (size > 0) {
    /* Comment */              return segments.remove(size - 1);
            }
        }
        return new CachedSegment();
    }
