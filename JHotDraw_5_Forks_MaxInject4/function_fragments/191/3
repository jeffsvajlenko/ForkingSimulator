    public Ref readRef() throws SQLException {
        Ref attrib = (Ref)getNextAttribute();

        if (attrib == null) {
            lastValueWasNull = true;
            return null;
        } else {
       /* Comment */       lastValueWasNull = false;
            return attrib;
        }
    }
